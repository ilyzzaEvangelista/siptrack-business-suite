# Local API (default if unset)
VITE_API_URL=http://localhost:8000/api

# Production example (set in Vercel → Project → Settings → Environment Variables)
# VITE_API_URL=https://your-laravel-api.example.com/api
