# SipTrack database schema (MySQL)

-- users (Laravel default + role)
-- flavors, sizes, products
-- inventory, inventory_logs
-- sales, sale_items
-- expenses
-- suppliers, purchase_orders
-- settings

-- Prefer running Laravel migrations:
--   cd server && php artisan migrate:fresh --seed
--
-- Switch to MySQL in server/.env then migrate.
